[build]
builder = "nixpacks"
buildCommand = "cd demo && bash mvnw -B package -DskipTests"

[deploy]
startCommand = "java -jar demo/target/demo-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/tasks"
restartPolicyType = "on_failure"
